.vscode/*
!.vscode/extensions.json
node_modules/
backend/app/frontend/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
packages/email/.bse-cache/

# SECURITY NOTE: .env files are still tracked on purpose (dev defaults only,
# upstream template style). Before any real deployment, uncomment the lines below and
# create .env.example templates so real secrets can never be committed.
# /.env
